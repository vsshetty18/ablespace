# Base URL of the deployed NestJS backend (Render).
# NEXT_PUBLIC_ prefix is required so this is readable in the browser.
# Set the real value in Vercel's Environment Variables dashboard —
# this file is a template only, never commit real URLs with secrets in them.
NEXT_PUBLIC_API_URL="https://your-backend.onrender.com"
